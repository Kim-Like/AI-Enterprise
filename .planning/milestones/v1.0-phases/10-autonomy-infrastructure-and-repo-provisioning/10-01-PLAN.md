---
phase: 10-autonomy-infrastructure-and-repo-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/IAn/Agent/AI-Enterprise/ops/*
  - /Users/IAn/Agent/AI-Enterprise/scripts/*
  - /Users/IAn/Agent/AI-Enterprise/api/*
  - /Users/IAn/Agent/AI-Enterprise/docs/*
  - /Users/IAn/Agent/AI-Enterprise/tests/*
  - /Users/IAn/Agent/AI-Enterprise/.env.example
autonomous: true
requirements:
  - AUT-01
  - AUT-03
  - AUT-04
  - AUT-05
user_setup: []
must_haves:
  truths:
    - "Repo provisioning extends the Phase 9 topology manifest instead of introducing a second inventory."
    - "Governed remote creation and bootstrap are idempotent, dry-run capable, and scoped to non-human service credentials."
    - "Wave 1 provisioning remains dry-run or preflight only until durable audit and rollback anchors exist."
    - "Autonomous provisioning is bounded by explicit kill-switch and policy settings before any write occurs."
  artifacts:
    - /Users/IAn/Agent/AI-Enterprise/ops/repository-topology.json
    - /Users/IAn/Agent/AI-Enterprise/scripts/provision_governed_remote.sh
    - /Users/IAn/Agent/AI-Enterprise/docs/autonomy-provisioning.md
    - /Users/IAn/Agent/AI-Enterprise/tests/test_phase10_contracts.py
  key_links:
    - "Phase 10 must extend the Phase 9 repo-topology manifest and Git-governance scripts rather than replace them."
    - "The existing admin versus autonomy write split remains the security boundary for routine non-human actions."
---

<objective>
Add the first executable autonomy layer on top of the Phase 9 Git-governance model.

Purpose: extend the repo-topology manifest with provisioning metadata, add a governed remote provisioning wrapper that reuses the existing bootstrap script, and enforce kill-switch or policy boundaries before autonomous writes.
Output: topology-driven provisioning contracts, dry-run capable repo reconcile scaffolding, preflight-only provisioning behavior for Wave 1, and explicit autonomy policy enforcement.
</objective>

<execution_context>
@/Users/IAn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/IAn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-autonomy-infrastructure-and-repo-provisioning/10-CONTEXT.md
@.planning/phases/10-autonomy-infrastructure-and-repo-provisioning/10-RESEARCH.md
@.planning/phases/10-autonomy-infrastructure-and-repo-provisioning/10-VALIDATION.md
@.planning/phases/09-infrastructure-topology-git-source-of-truth-and-deployment-governance-simplification/09-01-SUMMARY.md
@.planning/phases/09-infrastructure-topology-git-source-of-truth-and-deployment-governance-simplification/09-02-SUMMARY.md
@/Users/IAn/Agent/AI-Enterprise/ops/repository-topology.json
@/Users/IAn/Agent/AI-Enterprise/scripts/bootstrap_primary_remote.sh
@/Users/IAn/Agent/AI-Enterprise/scripts/validate_git_governance.sh
@/Users/IAn/Agent/AI-Enterprise/scripts/validate_ai_enterprise.sh
@/Users/IAn/Agent/AI-Enterprise/api/security/admin_auth.py
@/Users/IAn/Agent/AI-Enterprise/api/routes/settings.py
@/Users/IAn/Agent/AI-Enterprise/api/db/schema.sql
@/Users/IAn/Downloads/PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the repo-topology manifest with autonomy provisioning metadata</name>
  <files>/Users/IAn/Agent/AI-Enterprise/ops/*, /Users/IAn/Agent/AI-Enterprise/docs/*, /Users/IAn/Agent/AI-Enterprise/tests/*</files>
  <action>Extend `ops/repository-topology.json` so governed repositories carry provider, namespace, repo name, credential reference, creation eligibility, and autonomy scope metadata while preserving the existing env-based remote contract from Phase 9. Document the manifest contract and sync rule in autonomy docs and add topology contract tests.</action>
  <verify>cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=. pytest -q tests/test_phase10_contracts.py -k topology</verify>
  <done>The topology manifest remains the single desired-state inventory and contains enough data to drive governed repo creation without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Add a governed remote provisioning wrapper over the existing bootstrap primitive</name>
  <files>/Users/IAn/Agent/AI-Enterprise/scripts/*, /Users/IAn/Agent/AI-Enterprise/api/*, /Users/IAn/Agent/AI-Enterprise/docs/*, /Users/IAn/Agent/AI-Enterprise/tests/*, /Users/IAn/Agent/AI-Enterprise/.env.example</files>
  <action>Add a provider-aware provisioning or reconciliation path that reads the topology manifest, validates credential references, supports `dry_run`, and prepares governed remote creation for eligible repositories while delegating remote wiring to `bootstrap_primary_remote.sh` instead of reimplementing Git bootstrap logic. In Wave 1, keep live provisioning behind policy and audit readiness gates so this path is safe to run before durable audit plumbing lands in Plan 10-02.</action>
  <verify>cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=. pytest -q tests/test_phase10_contracts.py -k provisioning</verify>
  <done>Missing governed remotes can be derived from topology state, validated in dry-run mode, and prepared for idempotent bootstrap through the existing Git-governance scripts without enabling uncontrolled live writes.</done>
</task>

<task type="auto">
  <name>Task 3: Add autonomy policy and kill-switch enforcement before write paths</name>
  <files>/Users/IAn/Agent/AI-Enterprise/api/*, /Users/IAn/Agent/AI-Enterprise/docs/*, /Users/IAn/Agent/AI-Enterprise/tests/*</files>
  <action>Extend the settings or policy contract so autonomy mode, repo-provisioning enablement, and allowed repository scope are explicit, admin-managed, and enforced separately from break-glass admin writes. Keep the existing admin versus autonomy split intact and ensure policy checks run before autonomous provisioning attempts.</action>
  <verify>cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=. pytest -q tests/api/test_autonomy_api.py -k policy</verify>
  <done>Autonomous provisioning is bounded by explicit settings and kill-switch semantics, and routine automation cannot execute uncontrolled writes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=. pytest -q tests/test_phase10_contracts.py -k topology`
- [ ] `cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=. pytest -q tests/test_phase10_contracts.py -k provisioning`
- [ ] `cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=. pytest -q tests/api/test_autonomy_api.py -k policy`
- [ ] `cd /Users/IAn/Agent/AI-Enterprise && rg -n "create_if_missing|credential_ref|dry_run|preflight|AUTONOMY_" ops/repository-topology.json docs scripts api .env.example`
</verification>

<success_criteria>

- All tasks completed
- Repo provisioning extends `ops/repository-topology.json` instead of creating a parallel provisioning inventory
- Governed remote creation is dry-run capable, idempotent, and reuses `bootstrap_primary_remote.sh`
- Wave 1 leaves live provisioning in dry-run or preflight mode until durable audit plumbing exists
- Non-human credential references and autonomy policy are explicit in the runtime contract
- Kill-switch and repository-scope boundaries exist before autonomous writes are enabled

</success_criteria>

<output>
After completion, create `/Users/IAn/Agent/AI-Enterprise/.planning/phases/10-autonomy-infrastructure-and-repo-provisioning/10-01-SUMMARY.md`
</output>
