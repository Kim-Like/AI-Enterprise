---
phase: 07-live-wiring-validation-and-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/IAn/Agent/AI-Enterprise/api/app.py
  - /Users/IAn/Agent/AI-Enterprise/tests/api/test_frontend_delivery.py
  - /Users/IAn/Agent/AI-Enterprise/tests/api/test_live_cutover.py
autonomous: true
requirements:
  - UI-03
  - VAL-02
user_setup: []
must_haves:
  truths:
    - "The clean backend serves the built mission-control frontend with SPA fallback behavior."
    - "The duplicated system proves an IAn -> Program Master -> Specialist -> Engineer routing chain."
    - "API routes still return API responses and are not swallowed by the SPA delivery layer."
  artifacts:
    - "/Users/IAn/Agent/AI-Enterprise/api/app.py"
    - "/Users/IAn/Agent/AI-Enterprise/tests/api/test_frontend_delivery.py"
    - "/Users/IAn/Agent/AI-Enterprise/tests/api/test_live_cutover.py"
  key_links:
    - "The backend-served `/` route becomes the canonical AI-Enterprise dashboard entrypoint."
    - "Routing proof uses duplicated master/specialist ownership rather than synthetic placeholder agents."
---

<objective>
Deliver the clean dashboard from the clean backend and add a concrete end-to-end routing proof.

Purpose: remove the ad hoc dashboard serving path and satisfy the final orchestration proof requirement.
Output: backend SPA delivery plus automated end-to-end routing tests.
</objective>

<execution_context>
@/Users/IAn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/IAn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-live-wiring-validation-and-cutover/07-CONTEXT.md
@.planning/phases/07-live-wiring-validation-and-cutover/07-RESEARCH.md
@.planning/phases/07-live-wiring-validation-and-cutover/07-VALIDATION.md
@.planning/phases/06-mission-control-frontend-rebuild/06-02-SUMMARY.md
@/Users/IAn/Downloads/PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serve the built SPA from the clean backend</name>
  <files>/Users/IAn/Agent/AI-Enterprise/api/app.py</files>
  <action>Add static asset mounting and HTML fallback routes so `/`, `/programs`, `/orchestration`, `/agents/configs`, `/report`, `/secrets`, and `/settings` are served directly from `dist/` without swallowing `/api/*` routes.</action>
  <verify>cd /Users/IAn/Agent/AI-Enterprise && npm run build && PYTHONPATH=/Users/IAn/Agent/AI-Enterprise python3 -m pytest -p no:cacheprovider tests/api/test_frontend_delivery.py -q</verify>
  <done>The clean backend is the canonical dashboard host.</done>
</task>

<task type="auto">
  <name>Task 2: Prove IAn to program to specialist to engineer routing</name>
  <files>/Users/IAn/Agent/AI-Enterprise/tests/api/test_live_cutover.py</files>
  <action>Add an end-to-end test that triggers a flow owned by `father`, crosses a Program Master-owned governor and specialist, and ends in an Engineer-owned specialist with verifiable step order and ownership.</action>
  <verify>cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=/Users/IAn/Agent/AI-Enterprise python3 -m pytest -p no:cacheprovider tests/api/test_live_cutover.py -q</verify>
  <done>The duplicated hierarchy has a concrete end-to-end routing proof.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/IAn/Agent/AI-Enterprise && npm run build`
- [ ] `cd /Users/IAn/Agent/AI-Enterprise && PYTHONPATH=/Users/IAn/Agent/AI-Enterprise python3 -m pytest -p no:cacheprovider tests/api/test_frontend_delivery.py tests/api/test_live_cutover.py -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The backend-served dashboard loads from `/`
- End-to-end routing proof covers `father`, Program Master-owned specialists, and Engineer-owned specialists
</success_criteria>

<output>
After completion, create `/Users/IAn/Agent/AI-Enterprise/.planning/phases/07-live-wiring-validation-and-cutover/07-01-SUMMARY.md`
</output>
